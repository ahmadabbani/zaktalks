import OneOnOneContent from './OneOnOneContent'

export const metadata = {
  title: 'Private 1:1 Coaching | Zak Talks',
  description:
    'Private 1:1 coaching with Zak Dakkash. When the conversation gets hard, it means we are getting somewhere — book your session.',
}

export default function OneOnOnePage() {
  return <OneOnOneContent />
}
